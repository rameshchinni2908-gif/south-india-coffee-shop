services:
  - type: web
    name: south-india-coffee-shop-api
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build --workspace @south-india-coffee-shop/api && npm prune --omit=dev
    startCommand: npm run start --workspace @south-india-coffee-shop/api
    initialDeployHook: npm run seed:prod --workspace @south-india-coffee-shop/api
    healthCheckPath: /api/health
    autoDeployTrigger: checksPass
    buildFilter:
      paths:
        - apps/api/**
        - packages/shared/**
        - package.json
        - package-lock.json
        - render.yaml
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 15m
      - key: CLIENT_URL
        sync: false
      - key: SHOP_TIMEZONE
        value: Asia/Kolkata
      - key: TAX_PERCENTAGE
        value: 0
      # Kaapi Karts mini-game. Flip GAME_ENABLED in the Render dashboard to
      # release it; the socket server only starts when it is true.
      - key: GAME_ENABLED
        sync: false
      - key: GAME_ROOM_TTL_MINUTES
        value: 60
      - key: GAME_RESULT_TTL_HOURS
        value: 24
      - key: GAME_MAX_ROOMS_PER_IP_PER_HOUR
        value: 10
      - key: SEED_ADMIN_NAME
        sync: false
      - key: SEED_ADMIN_EMAIL
        sync: false
      - key: SEED_ADMIN_PASSWORD
        sync: false
