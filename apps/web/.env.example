VITE_API_BASE_URL=http://localhost:4000
VITE_SHOP_NAME=JRG South Indian Coffee Shop

# Kaapi Karts mini-game. Ships disabled; see KAAPI-KARTS.md.
# VITE_GAME_SOCKET_URL defaults to VITE_API_BASE_URL and must reach the API
# host directly, because the Vercel rewrite cannot carry a WebSocket upgrade.
VITE_GAME_ENABLED=false
# Order by message on the menu; also set ORDER_ASSISTANT_ENABLED=true on the API.
VITE_ORDER_ASSISTANT_ENABLED=false
VITE_GAME_SOCKET_URL=
