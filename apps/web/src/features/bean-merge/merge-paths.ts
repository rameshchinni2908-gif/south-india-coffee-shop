/**
 * Route path for the Bean Merge feature. Its own module so the page and the
 * route subtree can both import it without an import cycle.
 */

export const GAMES_PATH = "/games";
export const BEAN_MERGE_PATH = `${GAMES_PATH}/bean-merge`;
